[project]
name = "learn-python"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "python-pptx>=1.0.2",
    "pyyaml>=6.0.3",
]

[tool.uv.workspace]
members = [
    "learn_pathlib/cache_cleanup",
]
