[project]
name = "playground"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.141.1",
    "humanize>=4.16.0",
    "psycopg[binary,pool]>=3.3.5",
    "pyyaml>=6.0.3",
    "random-word>=1.0.13",
]

[tool.uv.workspace]
members = [
    "learn_docker/database",
]
